name: Get Recently Played Tracks

on:
  workflow_dispatch:
  schedule:
    - cron: '13 * * * *'

jobs:
  scheduled:
    runs-on: ubuntu-latest
    steps:
    - name: Check out repo
      uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.8
        cache: pip
    - name: Cache playwright 
      uses: actions/cache@v3
      with: 
        path: ~/.cache/ms-playwright/
        key: ${{ runner.os }}-browsers
    - name: Cache Spotify info
      uses: actions/cache@v3
      with: 
        path: ~/.config/spotify-git-scraper
        key: ${{ runner.os }}-spotify
    - name: Install python dependencies
      run: |-
        pip install -r requirements.txt
        playwright install chromium
    - name: Get tracks played in the last hour
      run: |-
        python app.py get-recently-played after "$(date +"%Y-%m-%d %T" -d "1 hour ago")" --limit 50 --time-zone UTC --write --filename recently_played.json
      env:
        SPOTIFY_CLIENT_ID: ${{ secrets.SPOTIFY_CLIENT_ID }}
        SPOTIFY_AUTH_STRING: ${{ secrets.SPOTIFY_AUTH_STRING }}
        USERNAME: ${{ secrets.USERNAME }}
        PASSWORD: ${{ secrets.PASSWORD }}
        REDIRECT_URI: ${{ secrets.REDIRECT_URI }}
    - name: Commit and push if it changed 
      run: |-
        git config user.name "Automated"
        git config user.email "actions@users.noreply.github.com"
        git add -A
        timestamp=$(date -u)
        git commit -m "Latest data: ${timestamp}" || exit 0
        git push